import * as React from "react";
import { Breadcrumb, Layout, Alert, Row, Col, Button } from "antd";

export function Loading() {
  return (
    <Row>
      <Col span={6} offset={6}></Col>
    </Row>
  );
}

export default Loading;
